apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: gcp-store
spec:
  provider:
    gcpsm:
      projectID: prodaccount
      auth:
        workloadIdentity:
          clusterLocation: us-central1-a # Your cluster zone
          clusterName: omnitrix-cluster # Your cluster name
          serviceAccountRef:
            name: external-secrets # The KSA name
            namespace: external-secrets # The namespace
