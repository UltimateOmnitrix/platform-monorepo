# apiVersion: pkg.crossplane.io/v1alpha1
# kind: ControllerConfig
# metadata:
#   name: gcp-config
# spec:
#   # ✅ Force the Pod to use the Service Account named 'provider-gcp'
#   # (This matches the default name Terraform usually sets up)
#   serviceAccountName: provider-gcp
#   args:
#     - --debug
