## backaste takes form inputs (paramets), maps them to values in fetch:template and replaces test-34-35 with the value entered by the user in the form

## mean the backstage directly (fetch:template) step copies everything inside the skeleton folder & process placeholders like test-34-35
apiVersion: storage.gcp.upbound.io/v1beta1
kind: Bucket
metadata:
  name: test-34-35
  namespace: default
  annotations:
    crossplane.io/external-name: test-34-35
spec:
  forProvider:
    location: US
    storageClass: STANDARD
  providerConfigRef:
    name: default
