## backaste takes form inputs (paramets), maps them to values in fetch:template and replaces temp-444-01 with the value entered by the user in the form

## mean the backstage directly (fetch:template) step copies everything inside the skeleton folder & process placeholders like temp-444-01
apiVersion: storage.gcp.upbound.io/v1beta1
kind: Bucket
metadata:
  name: temp-444-01
  namespace: default
  annotations:
    crossplane.io/external-name: temp-444-01
spec:
  forProvider:
    location: US
    storageClass: STANDARD
  providerConfigRef:
    name: default
